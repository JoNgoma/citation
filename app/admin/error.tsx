"use client"
import { Card, CardHeader, CardTitle } from "@/src/components/ui/card";

export default function Error() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>⚠️ Sorry error ⚠️</CardTitle>
      </CardHeader>
    </Card>
  );
}
